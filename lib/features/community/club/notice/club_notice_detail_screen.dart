import 'package:flutter/material.dart';

class ClubNoticeDetailScreen extends StatelessWidget {
  const ClubNoticeDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
