import 'package:flutter/material.dart';

class ClubNoticeScreen extends StatelessWidget {
  const ClubNoticeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
