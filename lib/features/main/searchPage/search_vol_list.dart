import 'package:flutter/material.dart';

class SearchVolList extends StatelessWidget {
  const SearchVolList({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('봉사 찾기',);
  }
}