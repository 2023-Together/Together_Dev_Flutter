import 'package:flutter/material.dart';

class UserInformInquiry extends StatelessWidget {
  const UserInformInquiry({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(
          "회원 정보",
        ),
      ),
    );
  }
}
